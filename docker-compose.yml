version: "3.9"
services:
  crm-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: crm_system
    ports:
      - "5173:5173"
    environment:
      - PORT=5173
      - HOST=0.0.0.0
    restart: unless-stopped